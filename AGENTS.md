# 本项目工作约束

## 发布边界

- 本项目永久只允许本地维护和本地发布。
- 允许执行本地构建、测试、打包、创建本地提交和本地标签。
- 禁止添加或配置任何 Git 远程仓库。
- 禁止执行 push、远程标签、远程 Release、远程 Pages、远程 PR 或任何其他网络发布操作。
- 禁止为远程发布读取、恢复或使用代码托管平台的账号凭据、令牌和登录状态。
- 必须保留本地提交历史、分支、标签和发布产物，不得因解除远程关联而删除本地内容。
- 除非用户以后明确撤销本约束，否则不得重新启用远程发布，也不得把远程发布作为默认建议。
